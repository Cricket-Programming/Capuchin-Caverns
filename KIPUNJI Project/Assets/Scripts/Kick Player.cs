using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class KickPlayer : MonoBehaviour
{


    /*
    This will not work, follow a tutorial to get a kickhammer
    private void OnCollisionEnter(Collision collision) 
    {
        if (collision.gameObject.ischildofgorillaplayer && !photonview.ismine)  //if the gameObject touching the object with tag Enemy Body
        {
            Kick();
        }    
    }    

    void Kick() //void means this method does not return anything
    {
        SceneManager.LoadScene(SceneManager.GetActiveScene().name);
    }   
    */
}
